# FSS0 Extractor

```
usage: fss0-extractor.py [-h] input output

positional arguments:
  input       Location of the fusee-secondary.bin file.
  output      Location to extract files

optional arguments:
  -h, --help  show this help message and exit
```